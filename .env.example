# AI coach (see components/CoachPanel.tsx) — falls back to authored hints if unset.
AI_GATEWAY_API_KEY=

# Supabase auth (see lib/supabase/) — login/signup show a "not connected" notice if unset.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
